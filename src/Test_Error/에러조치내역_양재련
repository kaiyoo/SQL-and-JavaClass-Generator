--------------------------- 주용민
1. 좌측 테이블명을 클릭안하고 TextArea에 Sql문을 치면 실행이안된다.
    >>
    원인 : LOG.debug("TableName : " + tblTableInfo.getSelectionModel().getSelectedItem().getTableName());
            로그 찍는 부분에서 선택된 테이블명을 호출하니 에러 발생
    조치 : 로그에 들어갈 내용을 수행되는 쿼리로 변경

2. TextArea에 Sql문을 여러개 치면 최하단(최신)Sql문만 실행되고 전에 쓰던 Sql문을 실행 할 수 없다.
    >>
    원인 : 구분자 ';' 기준으로 쿼리를 전송하는데 ';' 사이에 공백이 들어갈 경우 에러 발생
    조치 : 구분자로 쿼리 분리후 공백제거(.trim())  후 전송

3. 로그인후에 CRUD버튼 활성화 되있음(좌측 테이블명을 클릭안하고 누를시 제대로되지않은 팝업창 발생)
    >>
    원인 : 팝업 화면 구성시 테이블명이 필요한데  테이블명을 선택하지 않으니 널포인트 에러 발생
    조치 : 버튼 클릭시 테이블 선택 여부 확인하여 테이블 미선택시 안내 메시지 출력

4. 아이디를 클릭 안하고 로그인 할시 에러창을 띄운후 다시 로그인팝업창 뜨게 하기
    >>
    원인 : 로그인 버튼 클릭시 입력 항목 체크하는 부분이 빠져있어 잘못된 정보로 접속 시도함
    조치 : 로그인시 항목 체크후 안내 메시지 출력

5. 계정정보중 아무값이나 하나만 넣고도 로그인 할시 로그인이 된다.
    >>
    원인 : 잘못된 정보로 접속 시도시 기존 접속 상태가 유지되어 접속이 성공한것 처럼 보임
    조치 : 연결 실패시 안내 메시지 출력

6. 다른사람 localhost로 접속해서 로그인하면 로그인되고 CRUD버튼도 활성화된다.(대신 테이블값들은 자신의 테이블값들을 가지고있다)
    >>
    원인 : 잘못된 정보로 접속 시도시 기존 접속 상태가 유지되어 내용이 바뀌지 않음.
    조치 : 로그아웃시 디비 접속 완전 해제

7. File에서 생성해줄때 기존 파일이 있으면 '기존의 파일이 있습니다. 바꾸시겠습니까?' 라는 알람 메시지 뜨게하기

--------------------------- 박훈범
1. DML 입력팝업창에서 Enter 치면 바로 생성 (현재 버튼 클릭시에만 쿼리문이 만들어짐)
2. 파일 생성시 패키지명 미입력시 에러 발생
	>>
	조치 : 패키지명 체크하여 안내 메시지 출력
	
3. 패키지명만 입력후 생성버튼 누를시 파일생성 되었다는 알러트만 나오고 아무런 반응 없음
4. 로그인 디비 미선택후 삭제 버튼 누를시 에러 발생 ---> 완료

--------------------------- 오승민
1. selecttest 비교 연산자 사용시 따옴표( ' )가 연산자와 묶여서 나오고 '=' 이 무조건나옴.
    ex) WHERE DEP_CODE = '>203'
2. clear run 버튼 안됨.
3. 쿼리뜨는창 글씨가 너무작음.
4. 테이블 선택안하고 crud 선택시 창은 뜬다.
    (창이뜨고 취소를 눌러도 안꺼짐 ㅠㅠ x눌러야함 :: 에러창 떠서 아얘 안뜨게하는게 좋겠음. )
5. 업데이트창 현재 값이 보여졌으면 좋겠다.
6. 현재 type도 알려주고싶다.
7. 로그인 창에도 위에 저장된 로그인정보, 현재입력한정보 라는 설명? 이 있었으면 좋겠다.

--------------------------- 유혜수
1.SQL 생성 후 바로 run과 clear버튼이 활성화 되지 않고
   SQL 텍스트 필드에 엔터를 쳐야  run과 clear버튼이 활성화
2.run버튼 클릭 시 '쿼리문을 확인 하셨습니까? commit됩니다'라는 경고 메세지 출력하기
3.login 하지 않아도 test버튼 활성화
4.update 값을 입력하지 않고 SQL생성시 경고 메세지 출력하기



--------------------------- 한동균


--------------------------- 유홍상



